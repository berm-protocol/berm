{
  "name": "@berm/editor",
  "version": "0.1.0",
  "private": true,
  "description": "XOnly editor — write once, publish to Nostr, your node, and a native X Article.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "verify": "node verify.mjs",
    "serve": "node serve.mjs"
  },
  "devDependencies": {
    "@noble/curves": "^2.2.0",
    "@noble/hashes": "^2.2.0",
    "esbuild": "^0.28.1",
    "nostr-tools": "^2.24.0",
    "playwright": "^1.49.0",
    "ws": "^8.18.0"
  }
}
