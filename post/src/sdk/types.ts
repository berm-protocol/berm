/**
 * Re-export only. The canonical surface lives in ../../../sdk.
 *
 * Not a copy. Three copies of a signing interface is three versions of the
 * security properties, diverging quietly — the repository already learned this.
 */
export * from '../../../sdk/src/types.js';
