{
  "name": "@berm/graph",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "verify": "node verify.mjs",
    "serve": "node serve.mjs"
  },
  "devDependencies": {
    "@noble/hashes": "^2.2.0",
    "esbuild": "^0.28.1",
    "nostr-tools": "^2.24.0",
    "playwright": "^1.62.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "ws": "^8.21.1"
  }
}
