{
  "name": "@berm/bags",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "probe": "node probe.mjs",
    "build": "node build.mjs",
    "verify": "node verify.mjs"
  },
  "devDependencies": {
    "nostr-tools": "^2.24.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "esbuild": "^0.24.0",
    "playwright": "^1.48.0"
  }
}
